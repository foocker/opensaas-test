import { getEnabledMarketingNavItems, getEnabledDemoNavItems } from "../../../shared/config";

export const marketingNavigationItems = getEnabledMarketingNavItems();

export const demoNavigationitems = getEnabledDemoNavItems();
